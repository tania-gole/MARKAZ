{
  "name": "@markaz/core",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'noop: @markaz/core'",
    "dev": "echo 'noop: @markaz/core'",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests",
    "test:integration": "vitest run --config vitest.integration.config.ts --passWithNoTests",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@markaz/db": "workspace:*",
    "@markaz/types": "workspace:*",
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
